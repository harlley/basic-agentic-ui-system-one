# Project

React + TypeScript + Vite. Preserve the existing UI.
AI decisions use the public DiffusionGemma System One endpoint through src/lib/systemOne.ts.
Run bun run build and bun run lint before delivery.
